'use server';

/**
 * Server Actions for the Eisenhower Matrix feature.
 * Placeholder — wire up to your AI classification API.
 */
export async function classifyTask(formData) {
  // TODO: Call matrix-sort API route
  return { quadrant: 'q2' };
}
