import { RoadmapView } from '@/features/Goals';

export const metadata = {
  title: 'AI Roadmap Generator | SperoFlow',
  description: 'Generate a mathematically verified, hallucination-free prerequisite learning path using Graph RAG.',
};

export default function RoadmapPage() {
  return <RoadmapView />;
}
