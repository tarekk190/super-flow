import { AgenticHubView } from '@/features/AgenticHub';

export default function CalendarPage() {
  // Currently renders AgenticHubView (matching legacy CalendarPage behavior)
  return <AgenticHubView />;
}
